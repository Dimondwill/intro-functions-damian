import turtle
from turtle import *
t = Turtle()
t.shape('turtle')
t.speed(10000)
import math

'''def msqu(iRange):
    length=5
    angle=5
    for i in range(iRange):
      t.forward(length)
      t.left(90)
      t.forward(length)
      t.left(90)
      t.forward(length)
      t.left(90)
      t.forward(length)
      t.left(90)
      t.right(angle)
      length += 5
msqu(60)'''

'''def mstar(irange):
    length=25
    angle=144
    for i in range(irange):
        t.forward(length)
        t.left(angle)
        t.forward(length)
        t.left(angle)
        t.forward(length)
        t.left(angle)
        t.forward(length)
        t.left(angle)
        t.forward(length)
        t.left(angle)
        t.right(5)
        length += 5
mstar(60)'''







turtle.done()